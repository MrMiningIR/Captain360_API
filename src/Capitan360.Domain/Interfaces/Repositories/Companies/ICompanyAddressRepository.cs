namespace Capitan360.Domain.Interfaces.Repositories.Companies;

public interface ICompanyAddressRepository
{
    //Task<CompanyAddress?> GetByIdAsync(int addressId, CancellationToken cancellationToken);
    //Task<int> OrderAddress(int commandCompanyId, CancellationToken cancellationToken);

    //void AddToCompanyAddressContext(CompanyAddress companyAddress, CancellationToken cancellationToken);
}